name: Deploy Lambda Function

on:
  # Run this workflow from other workflows
  workflow_call:
    inputs:
      function:
        description: 'Name of function in AWS'
        required: true
        type: string
      directory:
        description: 'Location of function to upload'
        required: true
        default: 'kinesis-to-http'
        type: string
    secrets:
      aws_access_key_id:
        required: true
      aws_secret_access_key:
        required: true

jobs:
  deploy_lambda:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./${{ inputs.directory }}
    name: Deploy staging lambda
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-access-key-id: ${{ secrets.aws_access_key_id }}
          aws-secret-access-key: ${{ secrets.aws_secret_access_key }}
          aws-region: us-east-1
          role-session-name: gha-lambda-deploy

      - name: Install dependencies
        run: |
          pip install --target . requests

      - name: Zip package
        run: |
          zip -r ./lambda.zip .

      - name: AWS Deploy
        run: |
          aws lambda update-function-code \
            --function-name ${{ inputs.function }} \
            --zip-file fileb://lambda.zip
